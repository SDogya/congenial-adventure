defaults:
  - experiment_config   # loads ExperimentConfig dataclass (provides shape_meta, dataset_path defaults)
  - _self_
  - model: baseline
  - strategy: ddp_unused

seed: 42
batch_size: 32
learning_rate: 0.0003
