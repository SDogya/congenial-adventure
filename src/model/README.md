# Предобученные модели

| Файл | Содержимое |
|------|------------|
| `model.ckpt` | OAT-токенизатор, обучен ~100 эпох на LIBERO-10 (`H_l=8`) |
| `eval_mod.ckpt` | FD-DRAT политика, обучена ~6 из 10 запланированных эпох (сессия Kaggle была прервана досрочно) |

Оба чекпоинта получены в рамках прототипного запуска и используются по умолчанию при оценке. При переобучении пути необходимо обновить в соответствии с инструкциями в `CLAUDE.md`.
